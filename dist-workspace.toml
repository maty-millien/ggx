[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.32.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = ["shell"]
# Install a standalone updater with the application
install-updater = true
# Trigger releases through workflow dispatch
dispatch-releases = true
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "aarch64-unknown-linux-gnu", "x86_64-unknown-linux-gnu"]
# Where to host releases
hosting = "github"
# Extra steps injected before each build (relative to .github/workflows)
github-build-setup = "dist/build-setup.yml"
# The generated release workflow is hand-edited to drop the unused announce job
allow-dirty = ["ci"]
